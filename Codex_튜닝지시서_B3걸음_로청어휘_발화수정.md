# Codex 튜닝 지시서 — B 3걸음: 로청 전용 reason 발화 수정 + 재검증

> **대상 브랜치**: `codex/taxonomy-redesign-draft` (같은 draft, 이어서). **라이브(codex/voc-engine-rebuild)·main·deploy 금지. 초안만 — Cowork 재검증 후 확정.**
> 첨부: `로청 재검증용 VoC (귀곰 로니 후기 발췌).md` (43건) · `VoC Topic 택소노미 재설계 초안 v0.5`.

## 배경 (Cowork C2 검증 결과)
product_category=로봇청소기는 51/51 정상 도출됐으나, **로청 전용 reason 라벨이 발화를 안 함**. 예:
- "로청 바보 동선은 답이 없다, 동선 알고리즘 개선되면..." → **내구성/고장** ❌ (동선/주행이어야)
- "쇼파 아래 청소를 하지 않는 문제" → **사용 편의성(조작)** ❌ (동선/주행이어야)
- 51행 중 동선/주행·물걸레·스팀·장애물회피·배터리·먼지통 = **0건**.
→ trace상 내용은 이해했는데 라벨 선택이 common/generic으로 새는 문제.

## 수정 내용 (engine)
### 1. product_category=로봇청소기일 때 전용 reason 우선
- 로청 전용 어휘(동선/주행·청소흡입·물걸레·스팀·배터리·먼지통/자동비움·장애물 회피)를 **common보다 우선 후보로** 제시/가중.
- 로청 관련 내용이 명확하면 common(내구성/고장·사용편의성 등)으로 새지 않도록 지시 강화.

### 2. include/exclude 명확화 (특히 동선/주행)
- **동선/주행** — include: 동선·경로·못 가는 구역·쇼파/가구 아래·갇힘·경로 알고리즘·**인식/회피 실패**(전선·반려동물 배설물 등 특정 대상 못 피함) / exclude: 부품 파손·고장은 `내구성/고장`, 조작 UI 불편은 `사용 편의성`, 흡입 결과는 `청소/흡입 성능`.
- **장애물 회피** — include: 장애물·액체·전선·반려동물 배설물 회피 성공/실패 / exclude: 못 가는 구역·경로 자체는 `동선/주행`.

### 3. ★신규 라벨 추가 — `주행/문턱돌파` (실데이터 발견)
- 실제 로청 VoC에 **문턱·경사·매트 등반**이 매우 빈번(예: "4cm 문턱", "2cm 겨우 넘어감", "매트 못 올라감", "2.5cm 경사 잘 올라감"). 현 어휘에 없음 → **로청 전용 `주행/문턱돌파` 추가**.
  - include: 문턱·단차·경사·매트·층간소음매트 등반/주파 성공·실패, 돌파 높이(cm) 언급.
  - exclude: 실내 경로/구역은 `동선/주행`.
- (택소노미 문서 v0.5는 확정 배치 때 v0.6로 반영 — 이번엔 엔진에만.)

### 4. few-shot 예시 (프롬프트에 삽입)
- "로청 바보 동선, 동선 알고리즘 개선" → 동선/주행
- "쇼파 아래 청소 안 함" → 동선/주행
- "흰색 전선 인식 못함 / 강아지 쉬 못 피함" → 장애물 회피
- "4cm 문턱 못 넘음 / 매트 못 올라감" → 주행/문턱돌파
- "스팀 능력 좋다 / 스팀 배출 어디로" → 스팀
- "물걸레 압력 / 걸레판 청소 귀찮음" → 물걸레
- "배터리 문제로 스팀 방 하나 겨우" → 배터리
- "먼지봉투 처리 / 히든스테이션 먼지통" → 먼지통/자동비움 또는 소모품

## 재검증 (Codex 산출 → Cowork 판정)
- 첨부 **로청 재검증 VoC 43건**으로 새 엔진 실행. (원문 리스트 → 엔진 입력)
- **행별 CSV** 제출: `comment · product_category · topic · cej · inquiry_flag · tee_emotion_score · confidence_rating · trace_summary`.
- topic 분포 + **로청 전용 라벨 발화 건수** 보고.
- ★기대값/PASS 판정은 **Cowork가** 함(첨부 md 하단 '기대'는 Codex에 주지 말 것 — Codex는 순수 산출만).

## 제약 (하드)
- 같은 draft 브랜치 · 라이브/main/deploy 금지 · **확정 금지**(Cowork 재검증 후).
- 이번은 **로청 topic 발화만** 손봄(감성·CEJ·inquiry·타 제품군 로직 유지).
- 표준 실행 규칙: 서버 detach · LLM 배치 2분룰 미적용(백그라운드+진행로그) · 재시도 2회 · 막히면 부분결과라도 저장·보고.

## 보고
engine diff(어휘 우선순위·include/exclude·문턱돌파 추가·few-shot) + 재검증 43건 행별 CSV + 로청 전용 발화 분포. → Cowork 검증 후 확정 판단.
